[build.environment]
  NODE_VERSION = "14"

[build]
  publish = "dist"
  command = "npm run build"
  ignore = "git diff --exit-code --quiet $CACHED_COMMIT_REF $COMMIT_REF ."

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
